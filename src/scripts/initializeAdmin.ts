// Fichier temporairement désactivé pour éviter les erreurs de build
export const initializeAdmin = () => {
  return Promise.resolve();
};

export const isAdminSystemInitialized = () => {
  return false;
};

export const initializeAdminSystem = () => {
  return Promise.resolve();
};